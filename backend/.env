PORT=4000
NODE_ENV=development
BASE_URL=http://localhost:4000

DATABASE_URL="postgresql://postgres:Gunaking07@localhost:5432/rfp_db"

# SMTP (for nodemailer). Example using MailHog dev server (default)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=rfp@localhost

# AI (optional)
USE_OPENAI=false
OPENAI_API_KEY=

# File storage (local)
UPLOAD_DIR=./uploads
